# This project uses StandardRB for linting.
# Run: bundle exec standardrb
#
# This file exists for editor integration that expects .rubocop.yml
require:
  - standard
  - standard-rails

inherit_gem:
  standard: config/ruby-3.4.yml
